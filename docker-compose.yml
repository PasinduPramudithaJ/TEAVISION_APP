services:
  backend:
    build:
      context: ./Teavision
      dockerfile: Dockerfile
    container_name: teavision_app-backend
    ports:
      - "5000:5000"
    volumes:
      # Mount models directory (read-only) - adjust if models are large
      - ./Teavision/models:/app/models:ro
      # Mount uploads directory for persistence
      - ./Teavision/uploads:/app/uploads
      # Mount profile pictures directory for persistence
      - ./Teavision/profile_pictures:/app/profile_pictures
      # Mount database for persistence
      - ./Teavision/users.db:/app/users.db
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - teaqnet-network

  frontend:
    build:
      context: ./TeaQnet
      dockerfile: Dockerfile
    container_name: teavision_app-frontend
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - teaqnet-network

networks:
  teaqnet-network:
    driver: bridge

volumes:
  uploads:
  profile_pictures:
  users_db:

